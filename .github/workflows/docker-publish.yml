name: Docker Publish

on:
  workflow_dispatch:
  schedule:
    - cron: '0 10 * * *'
  push:
    tags: [ 'v*.*.*' ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Get latest Github release
        id: get_release
        uses: rez0n/actions-github-release@v2.0
        with:
          repository: AgoraIO-Community/agora-token-service
          type: latest
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Check if release is already in the README.md
        run: |
          if grep -q ${{ steps.get_release.outputs.release }} README.md; then
            echo "Tag (${{ steps.get_release.outputs.release }}) already in README.md"
            exit 0
          fi

      - name: Checkout the agora-token-service repo
        uses: actions/checkout@v2
        with:
          repository: AgoraIO-Community/agora-token-service
          ref: ${{ steps.get_release.outputs.release }}
          path: agora-token-service

      - name: Copy our Dockerfile
        run: cp Dockerfile agora-token-service/

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: agora-token-service
          push: true
          tags: iamriajul/agora-token-service:${{ steps.get_release.outputs.release }},iamriajul/agora-token-service:latest
          platforms: linux/amd64,linux/arm64

      - name: Append the version to README.md
        run: |
          echo - ${{ steps.get_release.outputs.release }} >> README.md

      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          file_pattern: 'README.md'




